# Build stage
FROM node:20-alpine as build

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies with legacy peer deps to handle socket.io-client
RUN npm install --legacy-peer-deps

# Copy the rest of the application code
COPY . .

# Build the application
RUN npm run build

# Production stage
FROM nginx:alpine

# Copy the built assets from build stage
COPY --from=build /app/dist /usr/share/nginx/html

# Copy nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Create the directory structure for templates
RUN mkdir -p /usr/share/nginx/html/templates

# Copy the template file
COPY src/templates/gitlab-device-script.py /usr/share/nginx/html/templates/

# Expose port 80
EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
